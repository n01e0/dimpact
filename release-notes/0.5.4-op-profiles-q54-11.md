# Q54-11 `precision-first` / `balanced` operational profiles

- Run ID: `791a60bd-36db-42b5-a573-7d230690c672`
- Task ID: `Q54-11`

## Scope
Add/lock operational confidence profile behavior for impact analysis (`--op-profile balanced|precision-first`) in regression tests.

## Changes
- Updated `tests/cli_impact_opts.rs` with profile-focused integration tests:
  - `cli_impact_op_profile_balanced_matches_min_confidence_inferred`
  - `cli_impact_op_profile_precision_first_matches_confirmed_plus_exclude_dynamic_fallback`

## Behavior fixed by tests
- `--op-profile balanced` behaves equivalently to `--min-confidence inferred`.
- `--op-profile precision-first` behaves equivalently to `--min-confidence confirmed --exclude-dynamic-fallback`.

## Verification
- `cargo test --offline cli_impact_op_profile_balanced_matches_min_confidence_inferred --test cli_impact_opts`
- `cargo test --offline cli_impact_op_profile_precision_first_matches_confirmed_plus_exclude_dynamic_fallback --test cli_impact_opts`
