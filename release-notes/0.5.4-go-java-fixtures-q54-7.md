# Q54-7 Go/Java extraction misclassification fixture additions

- Run ID: `791a60bd-36db-42b5-a573-7d230690c672`
- Task ID: `Q54-7`

## Added fixtures

- `tests/fixtures/go/analyzer_hard_cases_extraction_fp_points_v4.go`
  - Adds production-like extraction edge cases around method-value dispatch and pseudo-call noise in comments / string literals / raw strings.
  - Includes realistic call flow (`invoke`, `route.dispatch`, `r.inner.Handle`, `fmt.Sprintf`) while embedding false-positive bait (`Missing`, `Printf`, fake `Handle(...)` in literals).

- `tests/fixtures/java/analyzer_hard_cases_extraction_fp_points_v4.java`
  - Adds extraction edge cases around method references, lambda/function dispatch, and pseudo-call noise in comments / string literals / text blocks.
  - Includes realistic call flow (`router::dispatch`, `invoke`, `router.dispatch`, `fn.apply`) with false-positive bait (`missing`, `helper`, fake static calls in text block).

## Added regression tests

- `go_hard_case_fixture_extraction_fp_points_v4` (`src/languages/go_spec.rs`)
- `java_hard_case_fixture_extraction_fp_points_v4` (`src/languages/java_spec.rs`)

These tests assert expected unresolved refs are captured and common false-positive bait names are not extracted.
