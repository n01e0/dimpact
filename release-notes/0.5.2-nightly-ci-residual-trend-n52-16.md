# N52-16 nightly/CI summary residual trend integration

Run ID: `f8523962-c6b3-46f4-829d-a1863296a810`

## What was added

- New renderer script:
  - `scripts/render-strict-e2e-residual-trend.py`
  - input: `before.json`, `after.json` from `scripts/summarize-strict-e2e-skips.sh`
  - output: Markdown delta table (`env/server/capability/other` + residual lane summary)

- CI summary integration (`.github/workflows/CI.yml`)
  - engine_lsp regression lane now appends:
    1. migration snapshot
    2. residual classification snapshot
    3. residual trend table (HEAD^ -> HEAD)

- nightly summary integration (`.github/workflows/nightly-strict-lsp.yml`)
  - strict real-LSP nightly summary now appends:
    1. migration snapshot
    2. residual classification snapshot
    3. residual trend table (HEAD^ -> HEAD)

## Notes

- Both workflows now use checkout `fetch-depth: 2` to enable previous-commit comparison (`HEAD^`).
- If previous commit metadata is unavailable, workflow logic falls back to self-comparison (delta `0`).

## Sample trend (N52-14 -> N52-15)

- `totalSkipPrints`: `25 -> 19` (`-6`)
- `env`: `21 -> 15` (`-6`)
- `server`: `4 -> 4` (`+0`)
- `capability`: `0 -> 0` (`+0`)
- `actionableResidualLanes`: `0 -> 0` (`+0`)
