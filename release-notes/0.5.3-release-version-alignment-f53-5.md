# F53-5 release/tag and Cargo.toml version alignment

- Run ID: `a41dde90-e976-4d41-b878-305bffb4632b`
- Scope: resolve mismatch between published release/tag and crate version in `Cargo.toml`

## Observed mismatch
- Latest published release/tag: `v0.5.2`
- `Cargo.toml` package version (before): `0.5.1`

## Decision
- For F53-5, align crate version to latest published release baseline (`0.5.2`) rather than pre-bumping to `0.5.3`.
- This keeps release/tag and crate metadata consistent immediately.

## Changes
- `Cargo.toml`
  - `version = "0.5.1"` -> `version = "0.5.2"`

## Verification
- `cargo metadata --format-version 1 --no-deps` reports package `dimpact` version `0.5.2`.
