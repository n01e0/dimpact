# R51-2 High-Risk Dependency Validation (actions major / tree-sitter / rusqlite)

Run ID: `7ed9672a-9b42-4965-ad17-ebf608a1d70e`
Generated at: `2026-03-10` (JST)

## Scope
Validate high-compatibility-risk open dependency PRs first:

- Actions major updates: `#338`, `#339`, `#340`, `#342`, `#344`
- Rust crate high-risk: `#341` (tree-sitter group), `#343` (rusqlite)

## Validation method
1. Reviewed each PR check rollup (`gh pr view --json statusCheckRollup`).
2. For Rust crate high-risk PRs, reproduced locally on PR head SHA using temporary worktrees:
   - `cargo check -q`
   - `cargo test -q --test engine_lsp`

## Results

### Actions major PRs

| PR | Change | CI status | Validation note |
| --- | --- | --- | --- |
| [#338](https://github.com/n01e0/dimpact/pull/338) | `actions/setup-go` `v5 -> v6` | **UNSTABLE** (`bench-go-strict-lsp` failed) | Failed at benchmark workflow step **Install gopls**; other jobs mostly green. |
| [#339](https://github.com/n01e0/dimpact/pull/339) | `actions/download-artifact` `v4 -> v8` | CLEAN | Required checks green. |
| [#340](https://github.com/n01e0/dimpact/pull/340) | `actions/setup-node` `v4 -> v6` | CLEAN | Required checks green. |
| [#342](https://github.com/n01e0/dimpact/pull/342) | `actions/upload-artifact` `v4 -> v7` | CLEAN | Required checks green. |
| [#344](https://github.com/n01e0/dimpact/pull/344) | `docker/metadata-action` `v5 -> v6` | CLEAN | Required checks green. |

### Rust crate high-risk PRs

| PR | Change | CI status | Local reproduction |
| --- | --- | --- | --- |
| [#341](https://github.com/n01e0/dimpact/pull/341) | tree-sitter group update | **BLOCKED** (multiple CI failures) | Reproduced failure on PR head SHA `eca7c0f`: `cargo check -q` and `cargo test -q --test engine_lsp` both failed with `E0308` in `src/languages/rust_ts.rs` (`node.child` now expects `u32`, not `usize`). |
| [#343](https://github.com/n01e0/dimpact/pull/343) | `rusqlite` `0.37.0 -> 0.38.0` | CLEAN | Reproduced pass on PR head SHA `44f0c29`: both `cargo check -q` and `cargo test -q --test engine_lsp` passed. |

## Key evidence

- `#338` benchmark run: `bench-go-strict-lsp` failed at `Install gopls` step (run `22865918021`, job `66332584602`).
- `#341` local error excerpt:
  - `error[E0308]: mismatched types` in `src/languages/rust_ts.rs`
  - `node.child(i)` now requires `u32` index under updated tree-sitter API.

## Suggested handling order for next task (R51-3)

1. Treat `#341` as fix-required before merge (API adaptation needed in `rust_ts.rs`).
2. Treat `#338` as workflow-risk (investigate `gopls` install compatibility under `setup-go@v6`; may need workflow pin/adjustment).
3. Fast-track clean high-risk candidates first: `#343`, `#339`, `#340`, `#342`, `#344`.
