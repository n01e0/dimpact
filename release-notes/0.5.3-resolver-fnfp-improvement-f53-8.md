# F53-8 resolver FN/FP improvement

- Run ID: `a41dde90-e976-4d41-b878-305bffb4632b`
- Scope: resolver correction and FN/FP verification for newly added high-dynamic fixture paths

## Resolver fix
Adjusted Python dynamic resolver handling in `src/languages/py_spec.rs`:

- Keep tracking `setattr(..., attr_name, callable_ident)` monkeypatch bindings.
- Bridge those bindings back into dynamic `getattr/setattr` callsites that resolve the same `attr_name`.
- Emit inferred refs to the bound callable for those callsites, so caller-impact can traverse through monkeypatched dynamic dispatch paths.

## Verification case
Fixture: `tests/fixtures/python/analyzer_hard_cases_dynamic_monkeypatch_metaclass_protocol_v3.py`

Mutation used for measurement:
- `payload.strip().upper()` -> `payload.strip().lower()` (inside `patched_dispatch`)

Callers oracle for this case:
- `execute`
- `install_patch`
- `invoke_protocol`

### Before vs After (resolver)
| revision | impacted symbols | FN | FP |
| --- | --- | ---: | ---: |
| before (`1d97e99`) | `execute`, `install_patch` | 1 | 0 |
| after (this PR) | `execute`, `install_patch`, `invoke_protocol` | 0 | 0 |

- FN improved: `1 -> 0`
- FP kept: `0 -> 0`

Detailed machine-readable output:
- `release-notes/0.5.3-resolver-fnfp-improvement-f53-8.json`

## Regression guard
Updated golden baseline matrix (`tests/changed_impacted_golden_baseline.rs`) with a Python v3 monkeypatch case to lock this caller-impact behavior:

- changed: `patched_dispatch`
- impacted: `execute`, `install_patch`, `invoke_protocol`
