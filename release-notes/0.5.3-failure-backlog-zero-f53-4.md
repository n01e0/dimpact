# F53-4 Resolve remaining failures to backlog zero

Run ID: `418b4f96-7cea-4256-ac3e-af08c4763cc4`

## Goal
- Clear remaining failure backlog from F53-1/F53-2.
- Verify latest `main` required checks are green.
- Verify latest `nightly-strict-lsp` run is green after install-lane hardening.

## Fixes applied in this task
- Nightly workflow now ensures base tools/runtimes in-container before strict-LSP setup:
  - installs `git/bash/python3/nodejs/npm/ripgrep` via available package manager.
- TS/JS and Python install prechecks now verify runtime executability (`node -v` / `npm -v`), not only PATH presence.
- Kept npm retry path (`npm cache clean --force` + one reinstall) for transient install flake mitigation.

## Verification
- Filled after PR CI checks and manual nightly run complete.
