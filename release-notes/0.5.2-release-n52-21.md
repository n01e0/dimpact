# N52-21 Release Execution (`v0.5.2`)

Run ID: `f8523962-c6b3-46f4-829d-a1863296a810`

## Task
- After merge, create tag `v0.5.2`
- Publish GitHub Release `v0.5.2`

## Procedure
1. Merge PR with all required checks green.
2. Sync local `main` to merged head.
3. Create and push annotated tag `v0.5.2`.
4. Create GitHub Release `v0.5.2`.

## Result
- Filled after PR merge and release publication.
