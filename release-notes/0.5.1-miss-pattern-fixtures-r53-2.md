# R53-2 Major Miss Pattern Fixtureization

Run ID: `dbeebef7-ed73-453c-b1d4-3483147055fa`

This note captures fixture assets added from R53-1 corpus findings.

## Added fixtures

| Pattern ID | Language | Source signal (R53-1) | Fixture |
| --- | --- | --- | --- |
| MP-PY-01 | Python | `callees` lane: `FN impacted=9`, `FP edges=1` on python-heavy corpus | `tests/fixtures/python/analyzer_hard_cases_callees_chain_alias_return.py` |
| MP-RB-01 | Ruby | strict oracle lane blocked by `lsp initialize timeout or invalid response`; still need equivalent chain fixture for follow-up | `tests/fixtures/ruby/analyzer_hard_cases_callees_chain_alias_return.rb` |

## Why these fixtures

- Both fixtures isolate the same structural shape seen in corpus:
  - linear call chain (`f00 -> ... -> f10`)
  - alias/temporary variable before return (`v = fXX`, `inc = 1`, `v + inc`)
- `MP-PY-01` is the primary regression target for R53-3 analyzer fix/re-measure cycle.
- `MP-RB-01` is pre-seeded so ruby strict-oracle comparisons can be resumed immediately once environment/LSP initialization becomes stable.
