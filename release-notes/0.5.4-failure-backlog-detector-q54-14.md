# Q54-14 failure backlog detector script (main required + nightly)

- Run ID: `791a60bd-36db-42b5-a573-7d230690c672`
- Task ID: `Q54-14`

## Added script
- `scripts/detect-failure-backlog.sh`

## Purpose
Detect current failure backlog from two sources:
1. **main required checks** (branch protection required contexts on latest completed main CI run)
2. **nightly strict-lsp** (sampled latest completed runs on main)

The script outputs both JSON/Markdown summaries and computes:
- `mainRequiredFailures`
- `nightlyFailureRuns`
- `failureBacklogTotal`

## Example invocation
```bash
scripts/detect-failure-backlog.sh \
  --repo n01e0/dimpact \
  --main-branch main \
  --ci-workflow CI \
  --nightly-workflow nightly-strict-lsp.yml \
  --nightly-sample 4 \
  --out-json release-notes/0.5.4-failure-backlog-detection-q54-14.json \
  --out-md release-notes/0.5.4-failure-backlog-detection-q54-14.md
```

## Current snapshot (this run)
- main required failures: `0`
- nightly failure runs: `3`
- failure backlog total: `3`
