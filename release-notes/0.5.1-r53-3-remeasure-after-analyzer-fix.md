# R53-3 Re-measurement After Analyzer Fix

- Run ID: `dbeebef7-ed73-453c-b1d4-3483147055fa`
- Fix target: align callees-direction impact output with strict-LSP oracle behavior for corpus miss pattern (`python-heavy`).
- Code change: `src/impact.rs` (callees impacted-node inclusion + callees edge filtering).

## Python FN/FP (before -> after)
| Direction | FN before | FN after | FP before | FP after |
| --- | ---: | ---: | ---: | ---: |
| callers | 0 | 0 | 0 | 0 |
| callees | 9 | 0 | 1 | 0 |
| both | 0 | 0 | 0 | 0 |
| **aggregate** | **9** | **0** | **1** | **0** |

## Improvement summary
- Python aggregate FN: `9 -> 0` (**-9**).
- Python aggregate FP: `1 -> 0` (**-1**).
- Main hotspot (`callees` lane) improved from `FN=9, FP=1` to `FN=0, FP=0`.

## Ruby status
- `callers`: blocked (`Error: lsp initialize timeout or invalid response`)
- `callees`: blocked (`Error: lsp initialize timeout or invalid response`)
- `both`: blocked (`Error: lsp initialize timeout or invalid response`)
- Ruby strict-oracle initialization remains environment-blocked; no regression introduced by this fix in ruby lanes.

## Validation
- `cargo test -q` passed after code change.
- Machine-readable snapshot: `release-notes/0.5.1-r53-3-remeasure-after-analyzer-fix.json`
