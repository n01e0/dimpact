# Q54-2 TS/JS lane promotion to fail-fast (preflight/gate update)

- Run ID: `791a60bd-36db-42b5-a573-7d230690c672`
- Task ID: `Q54-2`

## Scope
Promote TypeScript/JavaScript strict real-LSP lanes from skip-safe residual to fail-fast active lanes by updating nightly preflight/gate policy.

## Changes
- Updated `.github/workflows/nightly-strict-lsp.yml` lane policy:
  - `STRICT_LSP_ACTIVE_LANES` from `go` to `go,typescript,javascript`
- Preflight/retry gate fallback references were updated consistently to the new active-lane default.

## Effect
- TS/JS lanes are no longer policy-disabled in preflight by default.
- TS/JS now participate in strict lane gating (same path as existing active fail-fast lanes).
- Non-promoted lanes (java/ruby/python) remain skip-safe unless explicitly added to `STRICT_LSP_ACTIVE_LANES`.

## Input reference
- Residual inventory used for promotion planning:
  - `release-notes/0.5.4-strict-real-lsp-skip-safe-inventory-q54-1.md`
