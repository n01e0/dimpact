# F53-10 nightly strict-lsp stabilization (install / preflight / retry / summary)

- Run ID: `a41dde90-e976-4d41-b878-305bffb4632b`
- Workflow: `.github/workflows/nightly-strict-lsp.yml`

## Problem observed
On branch validation run `22920586671`, strict preflight enabled multiple lanes (ts/js/java/go) and strict real-LSP tests failed repeatedly in unstable lanes (notably java and ts/js), causing finalize failure even though graduation checks were green.

## Stabilization changes

### Install stage
- Added explicit `id` to each install/health-check step (`install_ts_js`, `install_python`, `install_go`, `install_java`, `install_ruby`) so downstream gating/summaries can reference concrete outcomes.

### Preflight stage
- Added `id: preflight` and structured outputs:
  - `strict_lsp_any`
  - `strict_lsp_enabled_count`
  - per-lane flags (`strict_lsp_typescript`, `strict_lsp_javascript`, `strict_lsp_go`, `strict_lsp_java`, `strict_lsp_ruby`, `strict_lsp_python`)
- Preflight now evaluates lane gates from **install outcome + command health probe**.
- Introduced explicit lane policy env:
  - `STRICT_LSP_ACTIVE_LANES: "go"`
- Non-active lanes are kept skip-safe with clear reasons in preflight summary.

### Retry stage
- Retry strict step now re-evaluates lane gates after retry setup (so recovered installs can be reflected in retry execution).
- Retry lane re-evaluation respects the same `STRICT_LSP_ACTIVE_LANES` policy as preflight.

### Summary stage
- Added `Publish install/preflight stabilization summary` step with lane-by-lane install/preflight state.
- Extended retry-policy summary with preflight lane counts/flags context.

## Validation
- Failed (before policy hardening):
  - `22920586671` -> finalize failure due strict lane instability.
- Successful (after stabilization):
  - `22920877641` -> workflow **success**.

## Notes
- Ruby install (`gem install ruby-lsp`) can still fail intermittently in this container, but lane policy + preflight/retry/finalize handling now keeps nightly result stable and well-explained in summaries.
- Node20 deprecation warning for `actions/cache@v4` remains non-blocking and is tracked separately.
