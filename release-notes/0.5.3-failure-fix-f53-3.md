# F53-3 Top-priority failure fix (nightly strict-lsp install lane)

Run ID: `418b4f96-7cea-4256-ac3e-af08c4763cc4`

## Targeted failure (from F53-2)
- Primary cause category: `install`
- Dominant signal: `install-ts-js-lsp`
- Affected step: `Install + health-check TypeScript/JavaScript language server`

## Fixes applied
- `.github/workflows/nightly-strict-lsp.yml`
  - `actions/setup-node` runtime moved to Node `22`.
  - Added explicit precheck for `node`/`npm` availability before npm-based installs.
  - Hardened TypeScript/JavaScript language-server install with one retry (`npm cache clean --force` + reinstall).
  - Hardened Python `pyright` install path with the same retry pattern to reduce sibling install-lane flakiness.

## Expected impact
- Prevent empty `node/npm` runtime state from silently cascading into install failures.
- Reduce transient npm/network-related install flakes in nightly strict-lsp setup lanes.
